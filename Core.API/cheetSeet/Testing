# Create a new xUnit project named Notes.Service.Tests
dotnet new xunit -n Notes.Service.Tests

# Add the new project to your Solution file (.sln)
dotnet sln add Notes.Service.Tests

# Create a new xUnit project named Notes.Service.Tests
dotnet new xunit -n Notes.Service.Tests

# Add the new project to your Solution file (.sln)
dotnet sln add Notes.Service.Tests

# Install the Mocking Library (Moq) We need Moq to fake the database/repository efficiently.

dotnet add package FluentAssertions

dotnet add package Moq